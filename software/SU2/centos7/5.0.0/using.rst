Here is an example script:

.. literalinclude:: files/run_su2.sh
    :language: bash

Where we run on 2 nodes (48 MPI-ranks in total) with 60 minutes.

And submit the job using command::

  $ sbatch run_su2.sh


