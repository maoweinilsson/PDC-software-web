Here is an example script:

.. literalinclude:: files/jet_crf.sh
    :language: bash

Where we run on 8 nodes (256 processors in total) with 30 minutes.

And submit the job using command::

  $ sbatch jet_crf.sh


